# nagano-sky 改行修正版

## 修正内容

月の情報カードで、以下のように「地平線下」または「高度 約○°」と「昇 / 沈」の間に改行が入るよう、`app.js` を修正しました。

```text
細い月 / 月齢 約25.3 / 地平線下
昇 01:23 / 沈 13:45
```

## 修正箇所

`app.js` の `updateMoonInfo()` 内を、`textContent` ではなく `innerHTML` に変更し、`<br>` を入れています。
